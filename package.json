{
  "name": "jose-cv",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cd dashboard_src && npm install && npm run build && cd .. && mkdir -p dist/dashboard && cp -r dashboard_src/dist/* dist/dashboard/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
