{
  "name": "jose-cv",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "dashboard_src",
    "Attom"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build && (cd dashboard_src && npm run build) && mkdir -p dist/dashboard && cp -r dashboard_src/dist/* dist/dashboard/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
